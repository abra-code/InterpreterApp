# interp.models - main command for the model chooser window. Opening the window is the whole
# effect (via ACTIONUI_WINDOW); the init subcommand does the work. Per OMC guidance a
# non-blocking window's main script runs at an unpredictable time, so keep it a no-op.

exit 0
