# interp.new - File > New. Opens a fresh translator (two-editor) window via ACTIONUI_WINDOW; all
# setup happens in interp.window.init. Per OMC guidance a non-blocking window's main script runs
# at an unpredictable time, so keep it a no-op.

exit 0
